dist
.DS_Store
*.egg-info
build

# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtual environment
.venv/
venv/
env/

# Secrets & local config
.env
.env.local
.env.*.local
!.env.example

# Media outputs (generated locally)
*.mp4
*.srt
*.wav
!tests/fixtures/**/*.srt

# Voiceover probe output (Phase 0 local dev only)
voiceover_probe_output/

# Local debug & benchmark artifacts (keep scripts/benchmark_samples.json + docs)
debug/
artifacts/multi_sample_benchmark/
artifacts/translation_quality_review/

# Local deploy data (jobs, model cache) — never commit
data/
data/youtube-cookies.txt

# SA bridge runtime (config.json is committed)
.sa_bridge/state.json
.sa_bridge/last_sa_response.md
.sa_bridge/*.log
